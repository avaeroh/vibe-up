# Workflow Records

Store durable stage outputs here so later workflow stages can consult them without recreating the same context from scratch.

Recommended files:

- `initial_setup.md`
- `proposal.md`

Use the templates in `templates/` when creating or refreshing these records.
